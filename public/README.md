# ABCX logo

Place the official, unmodified ABCX logo at `public/abcx-logo.png`.

The page references that file directly and applies only responsive sizing (`object-fit: contain`); it does not recolor, crop, or otherwise transform the logo.
